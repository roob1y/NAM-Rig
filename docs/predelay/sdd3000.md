# Korg SDD-3000 — REMOVED (2026-07-04)

The SDD-3000 pre-amp delay model was **removed** from the code at Robbie's request — it wasn't
sounding right and adding controls/parameters wasn't going to fix it. The pre-amp delay pedal now
ships **three** models: Boss DD-7, MXR Carbon Copy, and EHX Deluxe Memory Man.

The research and prior build notes for the SDD-3000 remain in git history if it's ever revisited.
This file is a tombstone; it can be deleted.
