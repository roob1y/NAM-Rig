build/
build-*/
*.log
plate_proto/ir/
plate_proto/plate_rebuild_demos_v*/

# reverb voicing scratch (reference IRs, demos, analyzer) — local only, don't ship
hall_ref/
hall_v9/
plate_proto/*.f32
plate_proto/
plate_v9/
docs/PLATE_vintage plate_*.md

# python / compiled / binary artifacts (never commit)
__pycache__/
*.pyc
*.exe
*.obj
*.npz

# reverb-capture / optimizer dev artifacts (kept local)
plate_proto/bx20.wav
plate_proto/joint_*.json
plate_proto/jc_*.txt
plate_proto/py

# spring dev + research demos/harness (kept local, like the old spring_dev)
spring/

# reverb_analysis toolkit: track the code + docs, ignore reference IRs, renders,
# compiled drivers and the JUCE build stub (all local / regenerable)
reverb_analysis/ir/
reverb_analysis/*.f32
reverb_analysis/stub/
reverb_analysis/platefdn_driver
reverb_analysis/render_character
reverb_analysis/*.png
reverb_analysis/demos/
