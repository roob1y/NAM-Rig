# RESULTS — CORRECTION

An earlier draft here compared `PlateFdn` to the studio spring IRs and called it a "miss."
That was a wrong-premise analysis. The Plate (and Hall) are already shipped and
are NOT match targets, and the **studio spring IR set is the SPRING reference**.

Active work: matching the committed `SpringReverb` to the studio spring IRs.
See `RESULTS_spring.md`. The committed plate engine was never modified.
